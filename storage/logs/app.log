[2026-06-19 07:35:47] [INFO] 
            SELECT p.id,
                p.name as product_name,
                p.sku, 
                p.category_id,
                c.name as product_category,
                ss.warehouse_id,
                w.name as warehouse, 
                COALESCE(ss.quantity, 0) as stock,
                CASE 
                    WHEN COALESCE(ss.quantity, 0) = 0 THEN 'OUT'
                    WHEN COALESCE(ss.quantity, 0) < p.reorder_level THEN 'LOW'
                    ELSE 'OK'
                END as stock_status, 
                p.reorder_level,
                (
                    SELECT MAX(sm.created_at)
                    FROM stock_movements sm 
                    WHERE sm.product_id = p.id
                ) as last_movement_date
                
                FROM products p LEFT JOIN stock_snapshots ss 
                ON p.id = ss.product_id
                LEFT JOIN categories c 
                ON p.category_id = c.id 
                LEFT JOIN warehouses w 
                ON ss.warehouse_id = w.id 
                WHERE p.product_status = 'ACTIVE'
         AND stock_status = ?
[2026-06-19 07:35:47] [CRITICAL] 
        [mysqli_sql_exception]
MESSAGE: Unknown column 'stock_status' in 'where clause'
FILE: D:\XAMPP\htdocs\inventory-system\app\services\InventoryService.php
LINE: 713
TRACE: #0 D:\XAMPP\htdocs\inventory-system\app\services\InventoryService.php(713): mysqli->prepare('\r\n            S...')
#1 D:\XAMPP\htdocs\inventory-system\app\controllers\InventoryController.php(16): InventoryService::getInventoryOverviewData(Array)
#2 D:\XAMPP\htdocs\inventory-system\public\index.php(127): InventoryController->index()
#3 {main}

[2026-06-19 07:41:35] [INFO] 
            SELECT p.id,
                p.name as product_name,
                p.sku, 
                p.category_id,
                c.name as product_category,
                ss.warehouse_id,
                w.name as warehouse, 
                COALESCE(ss.quantity, 0) as stock,
                CASE 
                    WHEN COALESCE(ss.quantity, 0) = 0 THEN 'OUT'
                    WHEN COALESCE(ss.quantity, 0) < p.reorder_level THEN 'LOW'
                    ELSE 'OK'
                END as stock_status, 
                p.reorder_level,
                (
                    SELECT MAX(sm.created_at)
                    FROM stock_movements sm 
                    WHERE sm.product_id = p.id
                ) as last_movement_date
                
                FROM products p LEFT JOIN stock_snapshots ss 
                ON p.id = ss.product_id
                LEFT JOIN categories c 
                ON p.category_id = c.id 
                LEFT JOIN warehouses w 
                ON ss.warehouse_id = w.id 
                WHERE p.product_status = 'ACTIVE'
         AND (
                CASE 
                    WHEN COALESCE(ss.quantity, 0) = 0 THEN 'OUT'
                    WHEN COALESCE(ss.quantity, 0) < p.reorder_level THEN 'LOW'
                    ELSE 'OK'
                END
            ) = ?
[2026-06-19 07:41:47] [INFO] 
            SELECT p.id,
                p.name as product_name,
                p.sku, 
                p.category_id,
                c.name as product_category,
                ss.warehouse_id,
                w.name as warehouse, 
                COALESCE(ss.quantity, 0) as stock,
                CASE 
                    WHEN COALESCE(ss.quantity, 0) = 0 THEN 'OUT'
                    WHEN COALESCE(ss.quantity, 0) < p.reorder_level THEN 'LOW'
                    ELSE 'OK'
                END as stock_status, 
                p.reorder_level,
                (
                    SELECT MAX(sm.created_at)
                    FROM stock_movements sm 
                    WHERE sm.product_id = p.id
                ) as last_movement_date
                
                FROM products p LEFT JOIN stock_snapshots ss 
                ON p.id = ss.product_id
                LEFT JOIN categories c 
                ON p.category_id = c.id 
                LEFT JOIN warehouses w 
                ON ss.warehouse_id = w.id 
                WHERE p.product_status = 'ACTIVE'
         AND (
                CASE 
                    WHEN COALESCE(ss.quantity, 0) = 0 THEN 'OUT'
                    WHEN COALESCE(ss.quantity, 0) < p.reorder_level THEN 'LOW'
                    ELSE 'OK'
                END
            ) = ?
[2026-06-19 07:41:55] [INFO] 
            SELECT p.id,
                p.name as product_name,
                p.sku, 
                p.category_id,
                c.name as product_category,
                ss.warehouse_id,
                w.name as warehouse, 
                COALESCE(ss.quantity, 0) as stock,
                CASE 
                    WHEN COALESCE(ss.quantity, 0) = 0 THEN 'OUT'
                    WHEN COALESCE(ss.quantity, 0) < p.reorder_level THEN 'LOW'
                    ELSE 'OK'
                END as stock_status, 
                p.reorder_level,
                (
                    SELECT MAX(sm.created_at)
                    FROM stock_movements sm 
                    WHERE sm.product_id = p.id
                ) as last_movement_date
                
                FROM products p LEFT JOIN stock_snapshots ss 
                ON p.id = ss.product_id
                LEFT JOIN categories c 
                ON p.category_id = c.id 
                LEFT JOIN warehouses w 
                ON ss.warehouse_id = w.id 
                WHERE p.product_status = 'ACTIVE'
         AND (
                CASE 
                    WHEN COALESCE(ss.quantity, 0) = 0 THEN 'OUT'
                    WHEN COALESCE(ss.quantity, 0) < p.reorder_level THEN 'LOW'
                    ELSE 'OK'
                END
            ) = ?
[2026-06-19 07:42:00] [INFO] 
            SELECT p.id,
                p.name as product_name,
                p.sku, 
                p.category_id,
                c.name as product_category,
                ss.warehouse_id,
                w.name as warehouse, 
                COALESCE(ss.quantity, 0) as stock,
                CASE 
                    WHEN COALESCE(ss.quantity, 0) = 0 THEN 'OUT'
                    WHEN COALESCE(ss.quantity, 0) < p.reorder_level THEN 'LOW'
                    ELSE 'OK'
                END as stock_status, 
                p.reorder_level,
                (
                    SELECT MAX(sm.created_at)
                    FROM stock_movements sm 
                    WHERE sm.product_id = p.id
                ) as last_movement_date
                
                FROM products p LEFT JOIN stock_snapshots ss 
                ON p.id = ss.product_id
                LEFT JOIN categories c 
                ON p.category_id = c.id 
                LEFT JOIN warehouses w 
                ON ss.warehouse_id = w.id 
                WHERE p.product_status = 'ACTIVE'
         AND (
                CASE 
                    WHEN COALESCE(ss.quantity, 0) = 0 THEN 'OUT'
                    WHEN COALESCE(ss.quantity, 0) < p.reorder_level THEN 'LOW'
                    ELSE 'OK'
                END
            ) = ?
[2026-06-19 07:42:09] [INFO] 
            SELECT p.id,
                p.name as product_name,
                p.sku, 
                p.category_id,
                c.name as product_category,
                ss.warehouse_id,
                w.name as warehouse, 
                COALESCE(ss.quantity, 0) as stock,
                CASE 
                    WHEN COALESCE(ss.quantity, 0) = 0 THEN 'OUT'
                    WHEN COALESCE(ss.quantity, 0) < p.reorder_level THEN 'LOW'
                    ELSE 'OK'
                END as stock_status, 
                p.reorder_level,
                (
                    SELECT MAX(sm.created_at)
                    FROM stock_movements sm 
                    WHERE sm.product_id = p.id
                ) as last_movement_date
                
                FROM products p LEFT JOIN stock_snapshots ss 
                ON p.id = ss.product_id
                LEFT JOIN categories c 
                ON p.category_id = c.id 
                LEFT JOIN warehouses w 
                ON ss.warehouse_id = w.id 
                WHERE p.product_status = 'ACTIVE'
         AND (
                CASE 
                    WHEN COALESCE(ss.quantity, 0) = 0 THEN 'OUT'
                    WHEN COALESCE(ss.quantity, 0) < p.reorder_level THEN 'LOW'
                    ELSE 'OK'
                END
            ) = ?
[2026-06-19 07:42:14] [INFO] 
            SELECT p.id,
                p.name as product_name,
                p.sku, 
                p.category_id,
                c.name as product_category,
                ss.warehouse_id,
                w.name as warehouse, 
                COALESCE(ss.quantity, 0) as stock,
                CASE 
                    WHEN COALESCE(ss.quantity, 0) = 0 THEN 'OUT'
                    WHEN COALESCE(ss.quantity, 0) < p.reorder_level THEN 'LOW'
                    ELSE 'OK'
                END as stock_status, 
                p.reorder_level,
                (
                    SELECT MAX(sm.created_at)
                    FROM stock_movements sm 
                    WHERE sm.product_id = p.id
                ) as last_movement_date
                
                FROM products p LEFT JOIN stock_snapshots ss 
                ON p.id = ss.product_id
                LEFT JOIN categories c 
                ON p.category_id = c.id 
                LEFT JOIN warehouses w 
                ON ss.warehouse_id = w.id 
                WHERE p.product_status = 'ACTIVE'
         AND (
                CASE 
                    WHEN COALESCE(ss.quantity, 0) = 0 THEN 'OUT'
                    WHEN COALESCE(ss.quantity, 0) < p.reorder_level THEN 'LOW'
                    ELSE 'OK'
                END
            ) = ?
[2026-06-19 07:42:21] [INFO] 
            SELECT p.id,
                p.name as product_name,
                p.sku, 
                p.category_id,
                c.name as product_category,
                ss.warehouse_id,
                w.name as warehouse, 
                COALESCE(ss.quantity, 0) as stock,
                CASE 
                    WHEN COALESCE(ss.quantity, 0) = 0 THEN 'OUT'
                    WHEN COALESCE(ss.quantity, 0) < p.reorder_level THEN 'LOW'
                    ELSE 'OK'
                END as stock_status, 
                p.reorder_level,
                (
                    SELECT MAX(sm.created_at)
                    FROM stock_movements sm 
                    WHERE sm.product_id = p.id
                ) as last_movement_date
                
                FROM products p LEFT JOIN stock_snapshots ss 
                ON p.id = ss.product_id
                LEFT JOIN categories c 
                ON p.category_id = c.id 
                LEFT JOIN warehouses w 
                ON ss.warehouse_id = w.id 
                WHERE p.product_status = 'ACTIVE'
        
[2026-06-19 07:42:25] [INFO] 
            SELECT p.id,
                p.name as product_name,
                p.sku, 
                p.category_id,
                c.name as product_category,
                ss.warehouse_id,
                w.name as warehouse, 
                COALESCE(ss.quantity, 0) as stock,
                CASE 
                    WHEN COALESCE(ss.quantity, 0) = 0 THEN 'OUT'
                    WHEN COALESCE(ss.quantity, 0) < p.reorder_level THEN 'LOW'
                    ELSE 'OK'
                END as stock_status, 
                p.reorder_level,
                (
                    SELECT MAX(sm.created_at)
                    FROM stock_movements sm 
                    WHERE sm.product_id = p.id
                ) as last_movement_date
                
                FROM products p LEFT JOIN stock_snapshots ss 
                ON p.id = ss.product_id
                LEFT JOIN categories c 
                ON p.category_id = c.id 
                LEFT JOIN warehouses w 
                ON ss.warehouse_id = w.id 
                WHERE p.product_status = 'ACTIVE'
        
[2026-06-19 07:42:34] [INFO] 
            SELECT p.id,
                p.name as product_name,
                p.sku, 
                p.category_id,
                c.name as product_category,
                ss.warehouse_id,
                w.name as warehouse, 
                COALESCE(ss.quantity, 0) as stock,
                CASE 
                    WHEN COALESCE(ss.quantity, 0) = 0 THEN 'OUT'
                    WHEN COALESCE(ss.quantity, 0) < p.reorder_level THEN 'LOW'
                    ELSE 'OK'
                END as stock_status, 
                p.reorder_level,
                (
                    SELECT MAX(sm.created_at)
                    FROM stock_movements sm 
                    WHERE sm.product_id = p.id
                ) as last_movement_date
                
                FROM products p LEFT JOIN stock_snapshots ss 
                ON p.id = ss.product_id
                LEFT JOIN categories c 
                ON p.category_id = c.id 
                LEFT JOIN warehouses w 
                ON ss.warehouse_id = w.id 
                WHERE p.product_status = 'ACTIVE'
        
[2026-06-19 07:42:43] [INFO] 
            SELECT p.id,
                p.name as product_name,
                p.sku, 
                p.category_id,
                c.name as product_category,
                ss.warehouse_id,
                w.name as warehouse, 
                COALESCE(ss.quantity, 0) as stock,
                CASE 
                    WHEN COALESCE(ss.quantity, 0) = 0 THEN 'OUT'
                    WHEN COALESCE(ss.quantity, 0) < p.reorder_level THEN 'LOW'
                    ELSE 'OK'
                END as stock_status, 
                p.reorder_level,
                (
                    SELECT MAX(sm.created_at)
                    FROM stock_movements sm 
                    WHERE sm.product_id = p.id
                ) as last_movement_date
                
                FROM products p LEFT JOIN stock_snapshots ss 
                ON p.id = ss.product_id
                LEFT JOIN categories c 
                ON p.category_id = c.id 
                LEFT JOIN warehouses w 
                ON ss.warehouse_id = w.id 
                WHERE p.product_status = 'ACTIVE'
        
[2026-06-19 07:42:58] [INFO] 
            SELECT p.id,
                p.name as product_name,
                p.sku, 
                p.category_id,
                c.name as product_category,
                ss.warehouse_id,
                w.name as warehouse, 
                COALESCE(ss.quantity, 0) as stock,
                CASE 
                    WHEN COALESCE(ss.quantity, 0) = 0 THEN 'OUT'
                    WHEN COALESCE(ss.quantity, 0) < p.reorder_level THEN 'LOW'
                    ELSE 'OK'
                END as stock_status, 
                p.reorder_level,
                (
                    SELECT MAX(sm.created_at)
                    FROM stock_movements sm 
                    WHERE sm.product_id = p.id
                ) as last_movement_date
                
                FROM products p LEFT JOIN stock_snapshots ss 
                ON p.id = ss.product_id
                LEFT JOIN categories c 
                ON p.category_id = c.id 
                LEFT JOIN warehouses w 
                ON ss.warehouse_id = w.id 
                WHERE p.product_status = 'ACTIVE'
        
[2026-06-19 07:43:06] [INFO] 
            SELECT p.id,
                p.name as product_name,
                p.sku, 
                p.category_id,
                c.name as product_category,
                ss.warehouse_id,
                w.name as warehouse, 
                COALESCE(ss.quantity, 0) as stock,
                CASE 
                    WHEN COALESCE(ss.quantity, 0) = 0 THEN 'OUT'
                    WHEN COALESCE(ss.quantity, 0) < p.reorder_level THEN 'LOW'
                    ELSE 'OK'
                END as stock_status, 
                p.reorder_level,
                (
                    SELECT MAX(sm.created_at)
                    FROM stock_movements sm 
                    WHERE sm.product_id = p.id
                ) as last_movement_date
                
                FROM products p LEFT JOIN stock_snapshots ss 
                ON p.id = ss.product_id
                LEFT JOIN categories c 
                ON p.category_id = c.id 
                LEFT JOIN warehouses w 
                ON ss.warehouse_id = w.id 
                WHERE p.product_status = 'ACTIVE'
        
[2026-06-19 07:43:15] [INFO] 
            SELECT p.id,
                p.name as product_name,
                p.sku, 
                p.category_id,
                c.name as product_category,
                ss.warehouse_id,
                w.name as warehouse, 
                COALESCE(ss.quantity, 0) as stock,
                CASE 
                    WHEN COALESCE(ss.quantity, 0) = 0 THEN 'OUT'
                    WHEN COALESCE(ss.quantity, 0) < p.reorder_level THEN 'LOW'
                    ELSE 'OK'
                END as stock_status, 
                p.reorder_level,
                (
                    SELECT MAX(sm.created_at)
                    FROM stock_movements sm 
                    WHERE sm.product_id = p.id
                ) as last_movement_date
                
                FROM products p LEFT JOIN stock_snapshots ss 
                ON p.id = ss.product_id
                LEFT JOIN categories c 
                ON p.category_id = c.id 
                LEFT JOIN warehouses w 
                ON ss.warehouse_id = w.id 
                WHERE p.product_status = 'ACTIVE'
        
[2026-06-19 07:43:18] [INFO] 
            SELECT p.id,
                p.name as product_name,
                p.sku, 
                p.category_id,
                c.name as product_category,
                ss.warehouse_id,
                w.name as warehouse, 
                COALESCE(ss.quantity, 0) as stock,
                CASE 
                    WHEN COALESCE(ss.quantity, 0) = 0 THEN 'OUT'
                    WHEN COALESCE(ss.quantity, 0) < p.reorder_level THEN 'LOW'
                    ELSE 'OK'
                END as stock_status, 
                p.reorder_level,
                (
                    SELECT MAX(sm.created_at)
                    FROM stock_movements sm 
                    WHERE sm.product_id = p.id
                ) as last_movement_date
                
                FROM products p LEFT JOIN stock_snapshots ss 
                ON p.id = ss.product_id
                LEFT JOIN categories c 
                ON p.category_id = c.id 
                LEFT JOIN warehouses w 
                ON ss.warehouse_id = w.id 
                WHERE p.product_status = 'ACTIVE'
        
[2026-06-19 07:43:27] [INFO] 
            SELECT p.id,
                p.name as product_name,
                p.sku, 
                p.category_id,
                c.name as product_category,
                ss.warehouse_id,
                w.name as warehouse, 
                COALESCE(ss.quantity, 0) as stock,
                CASE 
                    WHEN COALESCE(ss.quantity, 0) = 0 THEN 'OUT'
                    WHEN COALESCE(ss.quantity, 0) < p.reorder_level THEN 'LOW'
                    ELSE 'OK'
                END as stock_status, 
                p.reorder_level,
                (
                    SELECT MAX(sm.created_at)
                    FROM stock_movements sm 
                    WHERE sm.product_id = p.id
                ) as last_movement_date
                
                FROM products p LEFT JOIN stock_snapshots ss 
                ON p.id = ss.product_id
                LEFT JOIN categories c 
                ON p.category_id = c.id 
                LEFT JOIN warehouses w 
                ON ss.warehouse_id = w.id 
                WHERE p.product_status = 'ACTIVE'
        
[2026-06-19 07:43:31] [INFO] 
            SELECT p.id,
                p.name as product_name,
                p.sku, 
                p.category_id,
                c.name as product_category,
                ss.warehouse_id,
                w.name as warehouse, 
                COALESCE(ss.quantity, 0) as stock,
                CASE 
                    WHEN COALESCE(ss.quantity, 0) = 0 THEN 'OUT'
                    WHEN COALESCE(ss.quantity, 0) < p.reorder_level THEN 'LOW'
                    ELSE 'OK'
                END as stock_status, 
                p.reorder_level,
                (
                    SELECT MAX(sm.created_at)
                    FROM stock_movements sm 
                    WHERE sm.product_id = p.id
                ) as last_movement_date
                
                FROM products p LEFT JOIN stock_snapshots ss 
                ON p.id = ss.product_id
                LEFT JOIN categories c 
                ON p.category_id = c.id 
                LEFT JOIN warehouses w 
                ON ss.warehouse_id = w.id 
                WHERE p.product_status = 'ACTIVE'
        
[2026-06-19 07:43:42] [INFO] 
            SELECT p.id,
                p.name as product_name,
                p.sku, 
                p.category_id,
                c.name as product_category,
                ss.warehouse_id,
                w.name as warehouse, 
                COALESCE(ss.quantity, 0) as stock,
                CASE 
                    WHEN COALESCE(ss.quantity, 0) = 0 THEN 'OUT'
                    WHEN COALESCE(ss.quantity, 0) < p.reorder_level THEN 'LOW'
                    ELSE 'OK'
                END as stock_status, 
                p.reorder_level,
                (
                    SELECT MAX(sm.created_at)
                    FROM stock_movements sm 
                    WHERE sm.product_id = p.id
                ) as last_movement_date
                
                FROM products p LEFT JOIN stock_snapshots ss 
                ON p.id = ss.product_id
                LEFT JOIN categories c 
                ON p.category_id = c.id 
                LEFT JOIN warehouses w 
                ON ss.warehouse_id = w.id 
                WHERE p.product_status = 'ACTIVE'
        
[2026-06-19 07:44:11] [INFO] 
            SELECT p.id,
                p.name as product_name,
                p.sku, 
                p.category_id,
                c.name as product_category,
                ss.warehouse_id,
                w.name as warehouse, 
                COALESCE(ss.quantity, 0) as stock,
                CASE 
                    WHEN COALESCE(ss.quantity, 0) = 0 THEN 'OUT'
                    WHEN COALESCE(ss.quantity, 0) < p.reorder_level THEN 'LOW'
                    ELSE 'OK'
                END as stock_status, 
                p.reorder_level,
                (
                    SELECT MAX(sm.created_at)
                    FROM stock_movements sm 
                    WHERE sm.product_id = p.id
                ) as last_movement_date
                
                FROM products p LEFT JOIN stock_snapshots ss 
                ON p.id = ss.product_id
                LEFT JOIN categories c 
                ON p.category_id = c.id 
                LEFT JOIN warehouses w 
                ON ss.warehouse_id = w.id 
                WHERE p.product_status = 'ACTIVE'
        
[2026-06-19 07:44:18] [INFO] 
            SELECT p.id,
                p.name as product_name,
                p.sku, 
                p.category_id,
                c.name as product_category,
                ss.warehouse_id,
                w.name as warehouse, 
                COALESCE(ss.quantity, 0) as stock,
                CASE 
                    WHEN COALESCE(ss.quantity, 0) = 0 THEN 'OUT'
                    WHEN COALESCE(ss.quantity, 0) < p.reorder_level THEN 'LOW'
                    ELSE 'OK'
                END as stock_status, 
                p.reorder_level,
                (
                    SELECT MAX(sm.created_at)
                    FROM stock_movements sm 
                    WHERE sm.product_id = p.id
                ) as last_movement_date
                
                FROM products p LEFT JOIN stock_snapshots ss 
                ON p.id = ss.product_id
                LEFT JOIN categories c 
                ON p.category_id = c.id 
                LEFT JOIN warehouses w 
                ON ss.warehouse_id = w.id 
                WHERE p.product_status = 'ACTIVE'
        
[2026-06-19 07:44:24] [INFO] 
            SELECT p.id,
                p.name as product_name,
                p.sku, 
                p.category_id,
                c.name as product_category,
                ss.warehouse_id,
                w.name as warehouse, 
                COALESCE(ss.quantity, 0) as stock,
                CASE 
                    WHEN COALESCE(ss.quantity, 0) = 0 THEN 'OUT'
                    WHEN COALESCE(ss.quantity, 0) < p.reorder_level THEN 'LOW'
                    ELSE 'OK'
                END as stock_status, 
                p.reorder_level,
                (
                    SELECT MAX(sm.created_at)
                    FROM stock_movements sm 
                    WHERE sm.product_id = p.id
                ) as last_movement_date
                
                FROM products p LEFT JOIN stock_snapshots ss 
                ON p.id = ss.product_id
                LEFT JOIN categories c 
                ON p.category_id = c.id 
                LEFT JOIN warehouses w 
                ON ss.warehouse_id = w.id 
                WHERE p.product_status = 'ACTIVE'
        
[2026-06-19 07:48:34] [INFO] 
            SELECT p.id,
                p.name as product_name,
                p.sku, 
                p.category_id,
                c.name as product_category,
                ss.warehouse_id,
                w.name as warehouse, 
                COALESCE(ss.quantity, 0) as stock,
                CASE 
                    WHEN COALESCE(ss.quantity, 0) = 0 THEN 'OUT'
                    WHEN COALESCE(ss.quantity, 0) < p.reorder_level THEN 'LOW'
                    ELSE 'OK'
                END as stock_status, 
                p.reorder_level,
                (
                    SELECT MAX(sm.created_at)
                    FROM stock_movements sm 
                    WHERE sm.product_id = p.id
                ) as last_movement_date
                
                FROM products p LEFT JOIN stock_snapshots ss 
                ON p.id = ss.product_id
                LEFT JOIN categories c 
                ON p.category_id = c.id 
                LEFT JOIN warehouses w 
                ON ss.warehouse_id = w.id 
                WHERE p.product_status = 'ACTIVE'
        
[2026-06-19 07:50:09] [INFO] 
            SELECT p.id,
                p.name as product_name,
                p.sku, 
                p.category_id,
                c.name as product_category,
                ss.warehouse_id,
                w.name as warehouse, 
                COALESCE(ss.quantity, 0) as stock,
                CASE 
                    WHEN COALESCE(ss.quantity, 0) = 0 THEN 'OUT'
                    WHEN COALESCE(ss.quantity, 0) < p.reorder_level THEN 'LOW'
                    ELSE 'OK'
                END as stock_status, 
                p.reorder_level,
                (
                    SELECT MAX(sm.created_at)
                    FROM stock_movements sm 
                    WHERE sm.product_id = p.id
                ) as last_movement_date
                
                FROM products p LEFT JOIN stock_snapshots ss 
                ON p.id = ss.product_id
                LEFT JOIN categories c 
                ON p.category_id = c.id 
                LEFT JOIN warehouses w 
                ON ss.warehouse_id = w.id 
                WHERE p.product_status = 'ACTIVE'
        
[2026-06-19 07:54:29] [INFO] 
            SELECT p.id,
                p.name as product_name,
                p.sku, 
                p.category_id,
                c.name as product_category,
                ss.warehouse_id,
                w.name as warehouse, 
                COALESCE(ss.quantity, 0) as stock,
                CASE 
                    WHEN COALESCE(ss.quantity, 0) = 0 THEN 'OUT'
                    WHEN COALESCE(ss.quantity, 0) < p.reorder_level THEN 'LOW'
                    ELSE 'OK'
                END as stock_status, 
                p.reorder_level,
                (
                    SELECT MAX(sm.created_at)
                    FROM stock_movements sm 
                    WHERE sm.product_id = p.id
                ) as last_movement_date
                
                FROM products p LEFT JOIN stock_snapshots ss 
                ON p.id = ss.product_id
                LEFT JOIN categories c 
                ON p.category_id = c.id 
                LEFT JOIN warehouses w 
                ON ss.warehouse_id = w.id 
                WHERE p.product_status = 'ACTIVE'
        
[2026-06-19 07:55:15] [INFO] 
            SELECT p.id,
                p.name as product_name,
                p.sku, 
                p.category_id,
                c.name as product_category,
                ss.warehouse_id,
                w.name as warehouse, 
                COALESCE(ss.quantity, 0) as stock,
                CASE 
                    WHEN COALESCE(ss.quantity, 0) = 0 THEN 'OUT'
                    WHEN COALESCE(ss.quantity, 0) < p.reorder_level THEN 'LOW'
                    ELSE 'OK'
                END as stock_status, 
                p.reorder_level,
                (
                    SELECT MAX(sm.created_at)
                    FROM stock_movements sm 
                    WHERE sm.product_id = p.id
                ) as last_movement_date
                
                FROM products p LEFT JOIN stock_snapshots ss 
                ON p.id = ss.product_id
                LEFT JOIN categories c 
                ON p.category_id = c.id 
                LEFT JOIN warehouses w 
                ON ss.warehouse_id = w.id 
                WHERE p.product_status = 'ACTIVE'
        
[2026-06-19 07:55:17] [INFO] 
            SELECT p.id,
                p.name as product_name,
                p.sku, 
                p.category_id,
                c.name as product_category,
                ss.warehouse_id,
                w.name as warehouse, 
                COALESCE(ss.quantity, 0) as stock,
                CASE 
                    WHEN COALESCE(ss.quantity, 0) = 0 THEN 'OUT'
                    WHEN COALESCE(ss.quantity, 0) < p.reorder_level THEN 'LOW'
                    ELSE 'OK'
                END as stock_status, 
                p.reorder_level,
                (
                    SELECT MAX(sm.created_at)
                    FROM stock_movements sm 
                    WHERE sm.product_id = p.id
                ) as last_movement_date
                
                FROM products p LEFT JOIN stock_snapshots ss 
                ON p.id = ss.product_id
                LEFT JOIN categories c 
                ON p.category_id = c.id 
                LEFT JOIN warehouses w 
                ON ss.warehouse_id = w.id 
                WHERE p.product_status = 'ACTIVE'
        
[2026-06-19 07:56:48] [INFO] User logged in: admin@example.com (ID: 1)
[2026-06-19 07:57:16] [INFO] User logged in: admin@example.com (ID: 1)
[2026-06-19 08:30:45] [INFO] User logged in: admin@example.com (ID: 1)
